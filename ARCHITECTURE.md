# 실행 구조 — 로컬(Mac) vs 클라우드(Cowork)

**원칙: 실제 고객 PII·자격증명·한국 서비스 접근은 로컬(Mac)에만. 클라우드는 코드만.**

```
┌─ 클라우드 (Claude Cowork / GitHub) ──────────────┐
│  · 코드·기능 개발(엔진·앱·리포트·리뷰)            │
│  · 테스트(합성/데모 데이터 minji)                 │
│  · 문서·디자인                                    │
│  → git push  (코드만, PII 없음)                   │
└───────────────────────┬──────────────────────────┘
                        │ GitHub (public/private repo — 코드만)
                        ▼
┌─ 로컬 (Mac, 매장 PC) ─────────────────────────────┐
│  git pull → 실제 데이터로:                        │
│   · 핸드SOS 수집 (자격증명·실브라우저·한국 IP)     │
│   · 네이버 노출 측정 (지역검색·플레이스·지도순위)  │
│   · 데이터 처리 (import·병합·build_app·onboard)    │
│   · 발견케어 채점·처방                             │
│   · 매일/매주 자동 스케줄(launchd)                 │
│  → clients/ · dist_app/ · secrets/  (전부 로컬 전용)│
└───────────────────────────────────────────────────┘
```

## 무엇이 어디서

| 작업 | 로컬(Mac) | 클라우드(Cowork) | 이유 |
|---|:---:|:---:|---|
| 핸드SOS 수집 | ● | | 자격증명·브라우저·한국 IP |
| 네이버 노출·플레이스·지도순위 | ● | | 클라우드는 네이버 접근 차단(403) |
| import·병합·build_app·onboard | ● | | 실제 고객 PII 처리 |
| 발견케어 측정 → 채점·처방 | ● | | 측정은 로컬, 로직 개발은 클라우드 |
| 자동 스케줄(수집·측정) | ● | | 데이터가 로컬에 |
| 코드·엔진·앱·기능 개발 | | ● | PII 불필요 |
| 테스트(합성/데모) | ● | ● | 어디서든(PII 없음) |
| 리포트·설계·문서 | | ● | 코드/문서 |

## 절대 규칙
- **`clients/`, `dist_app/`, `secrets/`, `_raw/`, `*.log` 는 git 제외 = 클라우드로 안 감.**
  (실제 고객 데이터·자격증명은 로컬에만. 한국 개인정보 residency 준수.)
- 클라우드에서 데이터가 필요한 검증은 **데모(minji)·합성 데이터**로.
- 코드는 클라우드에서 개발 → push → Mac 에서 pull → 실데이터로 실행.

## 왜 이 분리가 맞나
- **네이버·핸드SOS는 클라우드에서 접근 불가**(한국 서비스 + 차단). 실측은 로컬 필수.
- **PII 로컬 전용**은 처음부터의 설계 원칙(신뢰·법적 안전).
- 클라우드(Cowork)는 **개발 속도**(코드·기능·리포트)에만 쓰면 두 장점을 다 취함.

## Claude Cowork 등록
GitHub repo 를 Cowork 에 연결 → 클라우드에서 코드/기능 작업, 결과를 push.
Mac 은 그 코드를 pull 받아 실데이터 파이프라인을 돌린다(이 문서의 로컬 열).
Mac 설치·스케줄은 `scripts/MAC.md`.
