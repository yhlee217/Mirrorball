# Mirrorball 실서비스 런치 계획

> ## ⚠ 2026-08-26 — HandSOS 수집 저빈도 재개 (리스크 인지)
>
> 2026-07-20 중단했던 수집을 **운영자 판단으로 재개**합니다. 아래 리스크를 **인지한 상태의 결정**이며,
> **주 1회(일요일 14:00 KST) · 고객 관리 용도**로만 돌립니다.
>
> HandSOS 이용약관(2026-06-01 시행) **제19조 1항 17호**는 크롤링·스크래핑과 "그러한 모든 시도"를
> **빈도·목적과 무관하게** 금지합니다 — 즉 주기를 늘리고 '고객관리용'으로만 써도 **위반은 해소되지 않습니다.**
> 위반 시 **사전 통지 없이 계약 해지·서비스 중지**(19조 서두)될 수 있고, **잃는 쪽은 매장**(예약·매출·고객 시스템 정지).
> 함께 걸리는 조항: 9호(상업적 이용)·8호(목적 외 이용)·19조 2항·20조 2항·23조 1항.
>
> **왜 재개하나 / 리스크 완화** — 매장 자기 계정·국내 IP·주 1회면 정상 사용과 구분이 어렵고, 지난 30분 주기에도
> 제재가 없었던 점으로 보아 능동 모니터링은 없는 것으로 판단. 빈도 축소는 **법적 위반을 없애지는 못하지만**
> 서버 부하·노출을 줄이는 실무적 완화다. **저확률·고피해 꼬리 리스크**로 관리한다(봇탐지 추가·분쟁·사이트 개편 시 노출 가능).
>
> - **근본 해소(권장 이행):** (주)예스오예스 **서면 승낙**, 정식 반출 경로(12조 '데이터이전'), 또는
>   **매장이 직접 내보낸 파일을 읽는 방식**으로 전환. 수집 계층은 어댑터로 추상화돼 있어 파일 업로드/공식 API로 교체 가능.
> - 문의처: dev@handsos.com · cto@handsos.com · 1544-9368
> - 되돌리려면: `launchctl unload ~/Library/LaunchAgents/com.mirrorball.collect.plist` (수집 즉시 중단, 앱은 기존 데이터로 정상 동작).
>
> ### 공백기 메꾸기 — 재개 시 딱 한 번 (2026-07-20 ~ 08-26, 약 37일 누락)
>
> 중단 기간의 거래가 비어 있어 매출·방문·이탈 판정이 어긋난다. 정기 수집(주 1회·14일 창)은 이 구멍을
> 못 메우므로, **재개 직후 넓은 창으로 1회만** 돌린다. 맥에서:
>
> ```bash
> cd ~/Desktop/Dev/Mirrorball && git pull origin claude/charming-planck-hmgazi
> SYNC_DAYS=60 FORCE=1 bash worker/run_mac.sh      # 60일 = 공백 37일 + 여유
> ```
>
> - `FORCE=1` 은 영업시간 게이트 무시(지금 즉시 실행), `SYNC_DAYS=60` 은 기본 14일 창을 이번만 넓힘.
> - **안전한 이유**: 거래는 `(tenant_id, ext_id)` 업서트라 중복이 안 생기고, 스테일 정리는 *요청한 창*이
>   아니라 **이번에 실제 수확된 행의 min~max 날짜**로만 범위를 잡는다(`sync_tenant.py`) → 창 밖 과거
>   백필분은 건드리지 않는다. 수확이 비면 정리 자체를 건너뛴다(H1/H2 가드).
> - 예약은 앞으로 31일치를 매번 새로 받으므로 공백 메꾸기가 불필요하다.
> - 끝나면 `runs/collect.out.log` 의 `OK <slug> {...}` 통계로 건수를 확인하고, 앱에서 8월 매출·방문이
>   채워졌는지 본다. 이후부터는 launchd 가 주 1회로만 돈다.

현재 상태: 앱(멀티테넌트·핵심 기능·노출 케어) + 수집(맥 launchd, 국내 IP) + 하드닝 1차(H1/H2/M1·RLS 잠금) 완료. **로컬에서 완전 동작**. 남은 건 "실제로 디자이너들이 각자 접속해서 쓰게" 만드는 것.

---

## Phase 0 — 지금 정리 (준비 운동)

- [ ] 미커밋분 커밋·푸시 (노출 케어: `web/app/api/ai`, `web/app/expose`, `web/lib/coach.ts`, `tab-bar`, `wrangler`)
- [ ] 마이그레이션 전부 적용 확인 (Supabase SQL Editor):
  - `0005_settings`(판정기준) · `0006_booking_staff`(예약 담당) · `0007_customer_memo`(카르테 메모) · `0008`(tenants 쓰기 잠금, 하드닝)
- [ ] 맥 수집 정상 확인 (`launchctl list | grep mirrorball`, `runs/collect.out.log`에 최근 OK)

## Phase 1 — 런치 필수 (이게 관문)

- [ ] **배포 (Cloudflare Pages)**: 지금은 로컬만이라 디자이너가 못 씀. 리포 연결 → 빌드(`npx @cloudflare/next-on-pages@1`) → env 4개(URL·ANON·SERVICE_ROLE·KEK) + `NEXT_PUBLIC_SITE_URL` + `nodejs_compat`
- [ ] **Supabase Auth**: 배포 도메인을 Site URL·Redirect URLs에 추가 (`/auth/callback`, `/auth/confirm`)
- [ ] **실제 로그인 발송**: 지금은 dev 매직링크(수동). 이메일 발송 켜기 — Supabase 내장(저볼륨 OK) 또는 무료 SMTP(Resend 등). 그 후 디자이너 3명 각자 이메일로 온보딩
- [ ] **AI 활성화**: Cloudflare Workers AI 바인딩 `AI` 추가 + `@cloudflare/next-on-pages` 런타임 의존성 → '문구 다듬기'가 진짜 LLM으로 (지금은 템플릿 폴백)
- [ ] **시크릿 로테이션**: 채팅에 노출된 `SUPABASE_SERVICE_ROLE_KEY`(재발급·쉬움) + `MIRRORBALL_KEK`(신규 KEK로 **테넌트 DEK만 재래핑** — PII 재암호화 아님, 스크립트로 가능). 로테이션 후 모든 env(맥·Cloudflare) 갱신

## Phase 2 — 런치 전 권장 (신뢰·법무)

- [ ] **개인정보처리방침 + 이용약관**: 고객 이름·전화 처리 → 한국 PIPA상 필요. 살롱=개인정보 관리자, 우리=수탁(처리)자 구조·위탁 계약·처리 목적/보관기간 명시. 공개 페이지에 링크
- [ ] **각 디자이너 소개 페이지**: 최주환·이다운 공개 프로필(/p/slug) 채우기 (현재 비어있음)
- [ ] **수집 실패 알림**: `secrets/stores.yaml`의 `notify_url`(슬랙/디스코드 webhook) → 수집 끊기면 즉시 알림
- [ ] **커스텀 도메인**(선택): 전문성·신뢰

## Phase 3 — 운영·확장 (런치 후)

- [ ] 에러 모니터링(Sentry 무료 티어), 정기 백업 확인(Supabase)
- [ ] 세션 쿠키 재사용(매 수집마다 재로그인 회피 — 워커 TODO), 맥 단일장애점 대응(수집 워치독)
- [ ] '손님' 워크인 데이터 레벨 정리(현재 화면 제외만)
- [ ] 노출 케어 2탄: 마케팅 할 일 체크리스트 → 인스타 인사이트 → 네이버 플레이스 연동
- [ ] 결제/구독(수익화 시) — LLM·인프라 비용은 디자이너당 월 몇 센트라 구독료에 흡수

---

### 지금 가장 임팩트: **Phase 1 배포** (이게 되면 디자이너가 실제로 쓰기 시작). 그다음 **법무(개인정보)**가 실서비스의 진짜 관문.
