너는 미용사 카피를 채점하는 엄격한 평가자다. 아래 '생성 카피'를 5축으로 채점하고,
가드레일 위반을 잡아내라. 정답 카피와 비교해 주관 품질도 판정한다.
관대하게 주지 마라 — 근거(인용) 없는 점수는 금지.

[5축 (각 1~5)]
A SEO·AI 인용성: 지역+시술 키워드의 자연 포함, 질문/명확한 답 구조, AI가 인용 가능한가.
B 전환력: 페인 공감 → 신뢰(근거) → 부드러운 방문 유도(CTA)로 이어지는가.
C 노하우 충실도: 아래 'RAG 원칙'이 표면이 아니라 구체 메커니즘으로 반영됐는가.
D 진정성/보이스: 사람이 쓴 듯한 자연스러움·고유성, 슬롭·상투구 부재.
E 사실 정합성: '사실'만 사용했는가, 환각·과장 0인가.

[가드레일 — 하나라도 해당하면 verdict="fail"]
- 환각/과장: '사실'에 없는 주장, 또는 금지표현({{ must_not_claim | join(', ') }}) 등장
- 일반론만으로 구성 / 키워드 스터핑 / RAG 원칙 0건 반영 / AI 슬롭이 지배적

[입력]
- 사실: {% for f in facts %}{{ f }}; {% endfor %}
- RAG 원칙: {% for p in rag_principles %}{{ p }}; {% endfor %}
- 필수 포함(SEO): {{ must_include | join(', ') }}
- 금지 주장: {{ must_not_claim | join(', ') }}
- 정답 카피(참고 기준): {{ reference_output }}

[채점할 생성 카피]
{{ generated }}

[출력 — 아래 JSON 객체만, 다른 말·코드펜스 없이]
{"scores":{"A":0,"B":0,"C":0,"D":0,"E":0},"weakest_axis":"C","failures":[{"axis":"C","quote":"인용문","why":"이유"}],"guardrail_violations":[{"rule":"규칙","quote":"인용문"}],"pairwise_vs_reference":"generated|reference|tie","verdict":"pass|fail"}
