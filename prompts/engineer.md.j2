너는 카피 생성 프롬프트를 개선하는 프롬프트 엔지니어다.
아래 '현재 생성 프롬프트'를, 가장 약한 축을 끌어올리도록 **딱 한 갈래만** 수정해
'개선된 생성 프롬프트' 전체를 출력하라.

[규칙]
- 가장 약한 축({{ weakest_axis }})을 표적으로 한 변경에 집중한다.
- 다른 축을 깎지 마라(회귀 금지). 가드레일(사실만/일반론금지/스터핑금지/노하우반영/슬롭금지)은 유지·강화만.
- Jinja 변수({{ '{{' }} ... {{ '}}' }})와 블록({% raw %}{% ... %}{% endraw %})을 그대로 보존하라. 망가뜨리면 빌드가 깨진다.
- 설명·머리말 없이, 교체될 프롬프트 본문 '전체'만 출력한다.

[현재 점수]
- 축 평균: {{ axis_mean }}
- 가장 약한 축: {{ weakest_axis }}

[관찰된 실패(인용)]
{% for f in failures %}
- ({{ f.axis }}) "{{ f.quote }}" — {{ f.why }}
{% endfor %}

[현재 생성 프롬프트]
-----
{{ current_prompt }}
-----

[개선된 생성 프롬프트 (전체, 이것만 출력)]
