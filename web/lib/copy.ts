/**
 * 사용자에게 보이는 공통 문구.
 *
 * 저장 실패 하나를 두고 '저장 실패' · '처리 실패' · '저장하지 못했어요…' 세 가지가
 * 돌아다녔다. 같은 일에 다른 말이 나오면 다른 일이 난 줄 안다.
 *
 * 원칙: 무엇이 안 됐는지 + 그래서 뭘 하면 되는지. '실패' 한 단어로 끝내지 않는다.
 */
export const MSG = {
  /** 서버에 저장하지 못했을 때 — 되돌릴 손잡이가 화면에 그대로 있는 경우. */
  saveFail: '저장하지 못했어요. 잠시 후 다시 눌러주세요.',
  /** 짧은 자리(버튼 안·토스트)에 넣는 실패 표시. 문장이 들어갈 폭이 없을 때만. */
  saveFailShort: '저장 안 됨 · 다시 눌러주세요',
  saved: '저장됐어요 ✓',
  /** 클립보드는 브라우저가 막을 수 있어 대안을 함께 준다. */
  copyFail: '복사 안 됨 · 길게 눌러 복사',
} as const;
