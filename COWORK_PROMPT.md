# Cowork 시작 프롬프트 (새 세션에 붙여넣기)

너는 **Mirrorball** 프로젝트의 개발을 돕는 Claude야. 이 클라우드(Cowork) 환경은 **코드·기능 개발 전용**이다.

## 프로젝트
1인 컨시어지가 소규모 미용실 디자이너에게 제공하는 done-for-you B2B 서비스.
- 주 클라이언트: **하예원**(살롱톤 부원장, 영등포시장역). 살롱톤은 여러 디자이너 매장(하예원·최주환·이다운·유림).
- 제공 서비스: ① **CRM 앱**(고객관리·챙길고객·VIP·교차판매·재방문신호·매출통계) ② **발견케어**(네이버/AI 노출 진단→처방→주간카드) ③ **핸드SOS 자동 수집**(담당별 분리).

## 저장소·워크플로우
- repo: `yhlee217/Mirrorball`, 작업 브랜치: `claude/charming-planck-hmgazi`(최신 작업이 여기; 이어서 개발).
- 커밋 메시지 끝에 기존 커밋과 동일한 `Co-Authored-By` / `Claude-Session` 푸터를 붙인다.
- 사용자가 시키기 전엔 PR 만들지 말 것. 커밋·푸시는 브랜치에.

## 실행 구조 — 반드시 지킬 것 (자세히: `ARCHITECTURE.md`)
- **이 클라우드에선 네이버·핸드SOS 접근 불가(403), 실제 고객 PII 없음.** 실측·데이터 처리는 사용자 **Mac(로컬)**에서 한다.
- 너는 **코드를 개발해 push** → 사용자가 Mac에서 `git pull` 받아 실데이터로 실행.
- **절대 규칙: `clients/`·`secrets/`·`dist_app/`·`_raw/`·`*.log` 는 git 제외 = 절대 커밋/업로드 금지**(고객 PII·자격증명, 한국 개인정보 residency 준수).
- 데이터가 필요한 검증은 **데모(`clients/minji`)·합성 데이터**로만.

## 지금까지 구축된 것 (요약)
- **핸드SOS 파이프라인**: 자동 로그인→전량 수확(페이지네이션 완성)→담당별 분리(라벨→실제이름 매핑)→import(중복 없는 그룹 병합)→build_app→앱. 자가치유(셀렉터), 부분수집 정직화, `_raw` 정리, `--all-designers`, Mac/Win 러너·스케줄.
- **발견케어(`expose.py`)**: 네이버 지역검색·AI(Claude CLI)·플레이스·**실제 지도순위**·인기스타일 갭 → 발견점수·처방(키워드/리뷰/사진)·주간 변화추적·'○○님께' 카드·카톡 텍스트. 프랜차이즈 밀집 후순위. 인스타 발견(`insta.py`, Graph API).
- **`onboard.py`**: 데이터에서 전문시술 자동 도출→발견케어 target 생성→앱 빌드→디자이너별 서비스 요약.
- 앱: `app/index.html`(PWA). 통계/카피 엔진, 음성메모(브라우저 STT).

## 개발 규칙
- **테스트 우선**: `python -m pytest -q`(현재 345개 통과). 새 로직엔 **결정적 테스트** 추가.
- **크로스플랫폼**: 경로는 `pathlib.Path`. Windows(`.bat`/`.ps1`, **ASCII-only**) + Mac(`.sh`/launchd) 러너 둘 다 유지.
- **정직성 원칙**: 측정 못 한 건 "미측정"(0으로 단정 금지). 없는 강점 날조 금지.
- 한국어 UX. 파이썬/JS 로직 중복 금지(드리프트 방지 테스트 있음).

## 시작 절차
1. 먼저 읽기: `ARCHITECTURE.md`, `scripts/MAC.md`, `scripts/HANDSOS_REVIEW.md`(개선안), `scripts/EXPOSE.md`, `scripts/HANDSOS_IMPROVEMENTS.md`.
2. `python -m pytest -q` 로 상태 확인.
3. 그다음 내 작업 지시를 기다려. (예: 새 기능, 리포트, 리팩터링 — 실데이터가 필요한 건 코드만 만들고 "Mac에서 실행" 안내)
